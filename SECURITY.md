# 安全政策

## 支持范围

Orbit Desktop 目前处于早期 MVP 阶段，尚未发布稳定版本。安全修复会优先应用到默认分支的最新代码；早期构建和旧提交不保证获得补丁。

| 版本 | 是否接受安全更新 |
| --- | --- |
| 默认分支最新版本 | 是 |
| 旧提交与非官方构建 | 否 |

## 私密报告漏洞

请不要通过公开 Issue、讨论区或 Pull Request 披露未修复的安全漏洞。

优先使用 GitHub 仓库的 **Security → Advisories → Report a vulnerability** 私密报告功能。如果该入口尚未启用，请联系仓库维护者，并只发送最小化的初始信息以建立私密沟通渠道。

报告中请包含：

- 漏洞类型和潜在影响
- 受影响的版本、提交或文件
- 可复现的最小步骤或概念验证
- 所需的系统条件和权限
- 你建议的缓解或修复方案（如有）

请勿在报告中包含真实用户的应用清单、个人路径、访问令牌、签名证书或其他不必要的敏感数据。

## 响应预期

维护者会尽力在 7 天内确认收到报告，并在完成初步评估后同步严重程度、修复计划或需要补充的信息。由于本项目由社区维护，具体修复时间取决于影响范围与可用资源。

在修复发布前，请给予维护者合理的协调时间。我们会在条件允许时公开致谢报告者；如希望匿名，请在报告中说明。

## 安全边界

Orbit Desktop 会在本机读取当前用户/系统开始菜单、用户/公共桌面快捷方式、Windows App Paths 注册入口及可识别的 UWP 应用信息；用户确认添加后，才会把入口保存在 Orbit 的本地资料库中。应用图标也只缓存在本机。以下问题尤其值得报告：

- 未经明确用户操作启动程序
- 命令、参数或路径注入
- 绕过 Electron 上下文隔离或获得非预期的 Node.js 能力
- 通过恶意快捷方式执行超出预期范围的操作
- 泄露本机应用清单、用户路径或其他隐私数据
- 删除、卸载或修改用户的真实应用（Orbit 的“移除”只能移除本地资料库条目）
- 打包或更新链路中的依赖投毒与供应链风险

第三方应用自身的漏洞、Windows 平台问题，以及仅存在于修改后的非官方构建中的问题通常不在本项目的修复范围内，但如果 Orbit Desktop 放大了其影响，仍欢迎私密报告。

## 用户安全建议

- 仅从可信来源获取 Orbit Desktop
- 构建前检查依赖和变更内容
- 不要运行来源不明的快捷方式或可执行文件
- Windows 对未签名开发构建发出警告时，请先确认构建来源
